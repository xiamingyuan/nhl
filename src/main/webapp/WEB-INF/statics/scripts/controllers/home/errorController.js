define(['../module'], function (controllers) {
    'use strict';

    controllers.controller('error', ['$scope', '$http', '$location', function ($scope, $http, $location) {

        $scope.init = function () {

        }

        $scope.init();
    }])
})